# Application Server Configuration
PORT=3000
NODE_ENV=development

# Database Connections
# Note: Use 'mongo' and 'redis' container service names when running via Docker Compose
# Use 'localhost' when running the Node app directly on your host machine
MONGO_URL=mongodb://localhost:27017/mydatabase
REDIS_URL=redis://localhost:6379